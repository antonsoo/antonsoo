node_modules/
*.log
.DS_Store
.preview/
_preview.html
scripts/.fonttest.mjs
scripts/.raster.cjs
scripts/.fonts/
scripts/fonts/.cache/
# Local-only image masters: the committed artifact is the baked b64 (e.g.
# scripts/thoth.b64), not the heavyweight source it is generated from.
assets/more_images/
CLAUDE.md
AGENTS.md
.claude/

# Local-only work tracking and agent tooling. This repo is PUBLIC: job-search
# context and agent scaffolding must never ship. `bd init` creates all three.
.beads/
.agents/
.codex/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/
